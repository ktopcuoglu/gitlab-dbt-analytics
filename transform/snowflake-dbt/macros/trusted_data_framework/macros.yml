version: 2

macros:
  - name: model_count_and_group_by_date
    description: '{{ doc("model_count_and_group_by_date") }}'
    arguments:
      - name: model_name
        type: string
        description: Name of the dbt model. Typically the file name.
      - name: date_column_to_group_by
        type: string
        description: Name of date column to group by.
